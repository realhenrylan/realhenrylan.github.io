import{_ as a,o as n,c as t,ag as i}from"./chunks/framework.DO6ntPSX.js";const u=JSON.parse('{"title":"Turning Local Documents into Verifiable Memory: Engineering RAG and Graph RAG in Mneme","description":"How Mneme turns a local-document Q&A prototype into a maintainable RAG system through hybrid retrieval, index consistency, evidence boundaries, and explicit security controls.","frontmatter":{"title":"Turning Local Documents into Verifiable Memory: Engineering RAG and Graph RAG in Mneme","description":"How Mneme turns a local-document Q&A prototype into a maintainable RAG system through hybrid retrieval, index consistency, evidence boundaries, and explicit security controls.","date":"2026-07-23T00:00:00.000Z","tags":["Python","RAG","Graph RAG","ChromaDB","TUI"],"lang":"en-US"},"headers":[],"relativePath":"blog/mneme-rag-engineering.md","filePath":"blog/mneme-rag-engineering.md","lastUpdated":1785115728000}'),s={name:"blog/mneme-rag-engineering.md"};function o(r,e,l,c,d,h){return n(),t("div",null,[...e[0]||(e[0]=[i(`<h1 id="turning-local-documents-into-verifiable-memory-engineering-rag-and-graph-rag-in-mneme" tabindex="-1">Turning Local Documents into Verifiable Memory: Engineering RAG and Graph RAG in Mneme <a class="header-anchor" href="#turning-local-documents-into-verifiable-memory-engineering-rag-and-graph-rag-in-mneme" aria-label="Permalink to &quot;Turning Local Documents into Verifiable Memory: Engineering RAG and Graph RAG in Mneme&quot;">​</a></h1><p>Many RAG projects start with a short pipeline: split documents into chunks, generate embeddings, retrieve a few passages, and ask an LLM to answer. That is enough to get a demo running. It is not enough to make the system dependable. The harder questions are more basic: what happens when a file changes? Can two files with the same name remain distinct? Can an answer be traced back to a page and a chunk? What stops an instruction inside a document from being treated as a system instruction?</p><p>Mneme is a local-document RAG system built around those questions. It is named after Mnemosyne, the Greek goddess of memory, and provides a Python CLI, a bilingual terminal UI, and two retrieval modes: Standard RAG and Graph RAG. This post focuses on the engineering decisions behind the project rather than repeating a feature list.</p><h2 id="_1-start-with-the-complete-data-path" tabindex="-1">1. Start with the complete data path <a class="header-anchor" href="#_1-start-with-the-complete-data-path" aria-label="Permalink to &quot;1. Start with the complete data path&quot;">​</a></h2><p>Mneme&#39;s core path looks like this:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Local files</span></span>
<span class="line"><span>  → path normalization, type and resource validation</span></span>
<span class="line"><span>  → document splitting with stable identities</span></span>
<span class="line"><span>  → ChromaDB semantic index + BM25 lexical index</span></span>
<span class="line"><span>  → RRF fusion and dynamic Top-K</span></span>
<span class="line"><span>  → PDF anchor enrichment / Graph RAG expansion</span></span>
<span class="line"><span>  → bounded context with sources and safety boundaries</span></span>
<span class="line"><span>  → LLM answer with verifiable citations</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br></div></div><p>The important design choice is that indexing, retrieval, context construction, and answer generation are not independent features. They share the same source identity and version model, and the final answer must be able to point back to a file, page, and chunk.</p><p>Mneme supports PDF, DOCX, Markdown, HTML, JSON, CSV, XML, YAML, common configuration files, and several source-code formats. Indexing happens locally, but retrieved passages are sent to the configured OpenAI-compatible endpoint when Mneme performs query decomposition, Graph RAG entity extraction, or answer generation. &quot;Local indexing&quot; therefore does not mean that data can never leave the machine; that boundary has to be explicit.</p><h2 id="_2-why-hybrid-retrieval" tabindex="-1">2. Why hybrid retrieval <a class="header-anchor" href="#_2-why-hybrid-retrieval" aria-label="Permalink to &quot;2. Why hybrid retrieval&quot;">​</a></h2><p>Pure vector retrieval is good at semantic similarity, but it is not always reliable for filenames, abbreviations, numbers, proper nouns, or exact phrases. Pure BM25 can miss a useful paraphrase. Mneme keeps both retrieval signals:</p><ul><li>Sentence Transformers produce embeddings, with ChromaDB providing semantic retrieval;</li><li>BM25 provides lexical retrieval and reuses tokenization for unchanged chunks;</li><li>RRF (Reciprocal Rank Fusion) combines the ranked lists instead of comparing incompatible raw scores.</li></ul><p>RRF is simple and explainable. It does not require semantic distances and BM25 scores to share the same scale. After fusion, Mneme selects a dynamic Top-K from score gaps: a clear gap becomes a natural cutoff, while configured lower and upper bounds still apply when the gap is not decisive.</p><p>This design also leaves retrieval quality measurable. Recall@k, MRR, and nDCG can be used as regression signals instead of relying on the impression that an answer &quot;feels better.&quot; The repository includes benchmark data and quality-gate helpers, so chunking, embedding, and fusion changes can be evaluated systematically.</p><h2 id="_3-graph-rag-is-for-relationships-not-just-a-larger-top-k" tabindex="-1">3. Graph RAG is for relationships, not just a larger Top-K <a class="header-anchor" href="#_3-graph-rag-is-for-relationships-not-just-a-larger-top-k" aria-label="Permalink to &quot;3. Graph RAG is for relationships, not just a larger Top-K&quot;">​</a></h2><p>When a question spans documents, people, organizations, or concepts, increasing Top-K usually adds noise. Mneme&#39;s Graph RAG pipeline works in stages:</p><ol><li>Ask the LLM to extract entities from chunks in batches.</li><li>Preserve the mapping from entities to chunks.</li><li>Count entity co-occurrences across chunks.</li><li>Filter accidental relationships with a minimum co-occurrence threshold and cap the number of entities that participate in edge construction per chunk.</li><li>Extract query entities and expand one hop through the graph.</li><li>Fuse graph candidates with the standard hybrid-retrieval results using <code>alpha</code>.</li></ol><p>The constraints matter. An early fully connected subgraph could create many weak edges for a chunk containing many entities. The graph looked rich but retrieved noisily. Mneme now preserves the complete entity-to-chunk mapping while using co-occurrence thresholds and a per-chunk entity cap for edge construction. &quot;Where did an entity occur?&quot; and &quot;Which entities deserve an edge?&quot; are separate decisions.</p><p>If the graph is empty, Graph RAG falls back to standard semantic/hybrid retrieval instead of making an empty graph block the pipeline. Entity extraction also follows the current model and endpoint configuration; when API key or Base URL changes during a long-lived TUI session, the Graph RAG client refreshes with the configuration fingerprint.</p><h2 id="_4-the-most-underestimated-rag-problem-index-consistency" tabindex="-1">4. The most underestimated RAG problem: index consistency <a class="header-anchor" href="#_4-the-most-underestimated-rag-problem-index-consistency" aria-label="Permalink to &quot;4. The most underestimated RAG problem: index consistency&quot;">​</a></h2><p>The most dangerous vector-store bug is often not low recall. It is retrieving content that should no longer exist. Mneme does not use a basename or the document body as the unique identity. It uses a shared identity model:</p><ul><li>a normalized absolute path is used to derive <code>source_id</code>;</li><li>the file content is tracked with SHA-256;</li><li>every chunk has a stable <code>chunk_id</code>;</li><li>every collection has a versioned manifest;</li><li>ChromaDB, BM25 snapshots, Graph RAG caches, and citation records use the same identity model.</li></ul><p>That supports several awkward but common cases: two directories can contain <code>report.pdf</code> with identical text and still remain separate; changing one file replaces only its own chunks; deleting a file deletes the exact source rather than every file with the same basename; and a failed update rolls the collection, manifest, and BM25 snapshot back together.</p><p>An index update is therefore more than one <code>upsert</code>. A source mutation has to keep vector data, lexical retrieval, the manifest, graph cache, and query snapshots aligned. Treating the mutation as one recoverable unit prevents a half-successful state such as &quot;the vector store is new, but the manifest is old.&quot;</p><h2 id="_5-citations-are-part-of-the-context-protocol" tabindex="-1">5. Citations are part of the context protocol <a class="header-anchor" href="#_5-citations-are-part-of-the-context-protocol" aria-label="Permalink to &quot;5. Citations are part of the context protocol&quot;">​</a></h2><p>Mneme creates query-local citation IDs such as <code>S1</code> and <code>S2</code>. A citation record includes the source path, display name, PDF page, and chunk ID, so <code>[S1]</code> in an answer can be traced to a concrete piece of evidence.</p><p>More importantly, retrieved text is placed inside an explicit untrusted-document boundary:</p><div class="language-text vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>[Source: report.pdf] [Citation: S1]</span></span>
<span class="line"><span>&lt;untrusted_document chunk_id=&quot;source-a_chunk_3&quot;&gt;</span></span>
<span class="line"><span>Retrieved text ...</span></span>
<span class="line"><span>&lt;/untrusted_document&gt;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br></div></div><p>The model is instructed to treat that section as data, not as instructions. Even if a document contains text such as &quot;ignore previous instructions,&quot; it should remain inside the untrusted-document region.</p><p>Context length is bounded; the default cap is 60,000 characters sent to the remote endpoint. The implementation cannot simply truncate the entire serialized string. If only an opening tag remains, the protocol boundary is incomplete. Mneme truncates only <code>document_text</code>, preserves the complete source marker, citation, and opening/closing boundary, and skips a chunk when the complete safety frame cannot fit. A safety boundary should not be the first thing sacrificed under a tight budget.</p><h2 id="_6-security-has-to-cover-files-endpoints-and-caches" tabindex="-1">6. Security has to cover files, endpoints, and caches <a class="header-anchor" href="#_6-security-has-to-cover-files-endpoints-and-caches" aria-label="Permalink to &quot;6. Security has to cover files, endpoints, and caches&quot;">​</a></h2><p>Mneme centralizes policy in the lightweight <code>src/security.py</code>, so the CLI, TUI, query decomposer, and Graph RAG share the same checks:</p><ul><li>remote endpoints require HTTPS by default; loopback addresses such as <code>localhost</code>, <code>127.0.0.1</code>, and <code>::1</code> may use HTTP;</li><li>non-local HTTP requires the explicit <code>MNEME_ALLOW_INSECURE_HTTP=1</code> override;</li><li>document size, PDF page count, and an optional allowed document root can be limited;</li><li><code>.env</code> is explicitly rejected as an indexable document;</li><li>Graph RAG caches use schema-validated atomic JSON rather than loading pickle;</li><li>a cache must match the current index fingerprint and manifest version.</li></ul><p>These controls protect different layers. Endpoint validation protects transport, resource limits control accidental large inputs, path restrictions control the data scope, JSON avoids executable deserialization in the knowledge base, and manifest versions prevent an old graph from being attached to a new index.</p><h2 id="_7-the-tui-makes-background-updates-predictable" tabindex="-1">7. The TUI makes background updates predictable <a class="header-anchor" href="#_7-the-tui-makes-background-updates-predictable" aria-label="Permalink to &quot;7. The TUI makes background updates predictable&quot;">​</a></h2><p>The CLI is useful for scripts and one-shot queries. Mneme&#39;s Rich TUI is designed for longer sessions: streaming answers, file management, settings, source display, Standard RAG / Graph RAG switching, and directory watching.</p><p>The watcher debounces file events. Actual add, modify, delete, and rebuild operations go through a single-worker <code>IndexQueue</code>. Queries do not read lists that are being mutated; they use an immutable snapshot containing the collection, BM25 index, documents, metadata, and manifest version selected for that query. This prevents one request from observing a new BM25 index, an old document list, and a graph from yet another version.</p><p>The tradeoff is slightly less write concurrency in exchange for a state model that is easier to reason about. For a personal knowledge base and small-to-medium document collections, &quot;no half-applied update, no silent failure, stable query versions&quot; is often more valuable than maximizing write parallelism.</p><h2 id="_8-test-the-boundaries-not-only-the-happy-path" tabindex="-1">8. Test the boundaries, not only the happy path <a class="header-anchor" href="#_8-test-the-boundaries-not-only-the-happy-path" aria-label="Permalink to &quot;8. Test the boundaries, not only the happy path&quot;">​</a></h2><p>The test suite covers more than whether one document can produce an answer. It also checks:</p><ul><li>whether same-name paths and duplicate text keep independent identities;</li><li>whether modify, delete, and failed upsert operations roll the manifest back correctly;</li><li>whether the Graph JSON schema, size, fingerprint, and version are validated;</li><li>whether low-evidence retrieval refuses before calling the LLM;</li><li>whether citations, PDF pages, and untrusted-document boundaries remain complete;</li><li>whether the index queue is serialized and snapshots are isolated from later mutations;</li><li>whether cleanup works across Windows and Linux, and real external LLM tests are explicitly enabled through <code>MNEME_RUN_INTEGRATION=1</code>.</li></ul><p>The default suite stays offline-safe. Tests that require an external service are opt-in. That keeps local development and CI independent of personal API credentials while turning security behavior into stable regression tests.</p><h2 id="conclusion-reliable-rag-keeps-uncertainty-inside-explicit-boundaries" tabindex="-1">Conclusion: reliable RAG keeps uncertainty inside explicit boundaries <a class="header-anchor" href="#conclusion-reliable-rag-keeps-uncertainty-inside-explicit-boundaries" aria-label="Permalink to &quot;Conclusion: reliable RAG keeps uncertainty inside explicit boundaries&quot;">​</a></h2><p>The main lesson from Mneme is that RAG quality is not determined by the retrieval model alone. It is determined jointly by identity, versioning, evidence, and security boundaries.</p><p>Hybrid retrieval improves useful recall. Graph RAG connects cross-document relationships. The manifest keeps the index aligned with changing files. Citations and untrusted-document boundaries make answers traceable. Endpoint and resource policies control the data flow. Queues and snapshots keep a long-running TUI consistent.</p><p>Mneme is still a local-first personal knowledge-base tool, not a guarantee that sensitive data can never leave the machine. Sensitive documents still require a trusted LLM endpoint or a controlled local service. That is exactly why the data boundary belongs in the architecture, not only in the README: it is one of the most durable engineering practices in the project.</p>`,45)])])}const m=a(s,[["render",o]]);export{u as __pageData,m as default};
